package dev.acorn.core.text

/**
 * Horizontal alignment for text rendering
 */
enum class TextAlign {
    /**
     * Text is aligned to the left of the transform position
     */
    LEFT,

    /**
     * Text is centered on the transform position
     */
    CENTER,

    /**
     * Text is aligned to the right of the transform position
     */
    RIGHT
}